// JavaScript comment
const name = "hello world"
const age = 42
const active = true
const nothing = null

/* Block comment */
function greet(user) {
    return `Hi, ${user.name}!`
}

class Animal {
    constructor(species) {
        this.species = species
    }
}
